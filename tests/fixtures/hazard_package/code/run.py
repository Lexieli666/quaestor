"""Not a subject. The loader requires a `code/` directory; nothing in the suite runs this file."""

raise SystemExit(
    "tests/fixtures/hazard_package is a package.yaml fixture for the loader, not a subject; "
    "the real hazard subject is subjects/msr_prepayment (Phase 4)"
)
