# BLOCKERS.md — three-strike casualties

An entry is written when a problem has defeated three genuine attempts. It records what was tried,
what the failure looked like, and what the run did instead; `PROGRESS.md` gets `[blocked]` on the
phase and the session moves on. An empty file is the good case.

None.
